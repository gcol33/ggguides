## Resubmission

This is a resubmission addressing reviewer comments from the initial submission.

## Changes from previous submission

* Added `@return` documentation to all S3 methods (7 methods total)
* Changed `\dontrun{}` to `\donttest{}` in examples that require suggested packages
* All examples can now be executed by users with suggested packages installed

## Test environments

* local: Windows 11, R 4.5.2
* win-builder (via devtools::check_win_devel())

## R CMD check results

0 errors ✔ | 0 warnings ✔ | 0 notes ✔

## Downstream dependencies

There are currently no downstream dependencies for this package.
